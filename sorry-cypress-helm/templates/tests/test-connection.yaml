apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sorry-cypress-helm.fullname" . }}-test-connection"
  labels:
    {{- include "sorry-cypress-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sorry-cypress-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
